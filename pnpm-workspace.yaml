# Allow dependency install scripts on CI/VPS (pnpm 10+).
# Without this, `pnpm install` asks for `pnpm approve-builds`.
dangerouslyAllowAllBuilds: true

ignoredBuiltDependencies:
  - sharp
  - unrs-resolver
